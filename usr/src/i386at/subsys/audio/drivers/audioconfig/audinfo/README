	@(#)README	7.1	10/22/97	12:21:42
This directory contains source code for a proram which reads the
ascii card database (config.asc) and converts it to a binary file
(config.dat).

------- stevegi@sco.com adds:

This directory also contains:

	ossres -- a utility that reads the 4Front databse and dumps
		Tcl output.  This utility is required by audioconfig.

