TARGET  = $(SUITE_ROOT)/pkg
DLPI_BIN = $(SUITE_ROOT)/nd-dlpi/bin
INCLUDE = -I${SUITE_ROOT}/nd-dlpi/inc -I${TET_ROOT}/inc/posix_c
LIBS   = -L${SUITE_ROOT}/nd-dlpi/lib -L${TET_ROOT}/lib/posix_c -ldl -lapi -lnsl -lelf
CFLAGS  = ${DFLAGS} ${INCLUDE} ${COPTS} 
TET_OBJ = ${TET_ROOT}/lib/posix_c/tcm.o
RM 	= rm -f
