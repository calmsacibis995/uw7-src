
This directory contains a simple build and clean tool
that executes smake (a front end to make that includes the
tetbuild.cfg parameters into the environment), and places
the results file into the journal.
