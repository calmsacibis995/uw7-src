#ident	"@(#)ihvkit:display/lfb256/README.2	1.2"

To generate a Makefile, use the command:

	/usr/X/bin/imake -DTOPDIR=. -DCURDIR=. -I/usr/X/lib/config

This will use the header files and libraries provided with this
package.  When you are ready to execute make(1), you must set
the DISPLIBREV variable (for example, "make DISPLIBREV=1").
