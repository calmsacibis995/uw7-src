#ident	"@(#)ihvkit:pdi/dditools/make.README	1.1"
#ident  "$Header$"

This directory contains the source files necessary to make the tool
ddicheck.  The make command line should include references to the
cmd.rulefile and is as follows:

   make -f ddicheck.mk install CMDRULES=./cmd.rulefile PFX= MACH= install
