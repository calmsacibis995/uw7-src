#ident	"@(#)set.8	16.1	98/03/03"
.pageno
Page %d of %d
.ul
UNIX System Installation
.hhelp_ban
Instructions
.helpbanner
Help: Remove Package
.ur
Remove Package
.ll
.lr
F1=Help
.top
`[ "$ALL_NEW" ] &&  echo "WARNING: Newer versions of all packages are installed."`
`[ "$ALL_NEW" ] ||  echo "WARNING: Newer versions of some packages are installed."`

`[ "$ALL_NEW" ] &&  echo "Newer versions of all packages that you selected in the '${SETINST}' set are\ninstalled on your computer.  You are installing older versions of these\npackages.  Press the 'F1' (or '?') key to see a list of the packages whose\nnewer versions are already installed."`
`[ "$ALL_NEW" ] ||  echo "Newer versions of some packages that you selected in the '${SETINST}' set are\ninstalled on your computer.  You are installing older versions of these\npackages.  Press the 'F1' (or '?') key to see a list of the packages whose\nnewer versions are already installed."`

You must remove the newer version of these packages before you can install
an older version.  Please save your configuration files before removing
the newer package(s).

The package(s) will not be installed at this time.
`[ "$ALL_NEW" ] &&  echo "Installation of this set will now be terminated."`
See the documentation for further information.
.form
ENTER
.selection
Press 'ENTER' to continue.
.help
For detailed instructions, press the 'F1' (or '?') key at any time.

`[ "$ALL_NEW" ] || echo "Newer versions of some packages in the '${SETINST}' set are already installed"`
`[ "$ALL_NEW" ] && echo "Newer versions of all packages in the '${SETINST}' set are already installed"`
on your computer.  These packages include:

`cat $PKG_RM`

These packages cannot be overlayed and must be removed before you can
install the older version.

It is important that the newer packages be removed first, before the
older packages can be installed.  Please save your configuration files
before removing the newer packages.

See the documentation for further information.
.helpinst
ESC=Exit help  F1=Instructions  1=Forward  2=Backward
.hhelpinst
ESC=Exit instructions  1=Forward  2=Backward
.working
Working...
.end
