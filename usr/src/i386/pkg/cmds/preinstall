#ident	"@(#)pkg.cmds:i386/pkg/cmds/preinstall	1.6.1.1"
#ident	"$Header$"

# Don't pdelete. Can't padd once it is pdeleted.

# Save volatile files during upgrade or overlay
SCRIPTS=/usr/sbin/pkginst
${SCRIPTS}/pkgsavfiles ${PKGINST}

exit 0
