#ident	"@(#)menu.5	1.3"
#ident  "$Header$"

.ul
Network Information Service (NIS) Installation and Configuration
.lr
F1=Help
.top
`[ "$NIS_DOMAIN" ] &&  echo "\n \n \nThe NIS domain name for this machine is: $NIS_DOMAIN\n \n"`
`[ "$NIS_DOMAIN" ] &&  echo "\nTo change the domain name, enter a new value below:\n"`
.bottom
Press 'TAB' to move the cursor between fields.  When finished, move the
cursor to "Apply" and then press 'ENTER' to continue.
.form


#`[ "$NIS_DOMAIN" ] && echo "2 1//YES::$NIS_DOMAIN//Enter host's NIS domain name://NIS_DOMAIN\n \n//"`
#`[ "$NIS_DOMAIN" ] && echo "// Left/Right arrow keys for next choice (2 choices)//"`
5 1 30//[A-z0-9 ]*//NIS domain name://NIS_DOMAIN//
//Press enter to accept current value for the NIS domain name //


.button
Apply
Reset
#`[ "$NIS_DOMAIN" ] echo "2 1 //:: $DEF_DOMAIN//Enter local host's NIS domain name ://NIS_DOMAIN//"`
#2 1//:://Enter local host's NIS domain name ://NIS_DOMAIN//
#//To accept current domain name press enter //
#
#
.help
Before you configure your host for NIS, you must determine the NIS
domain into which your host will be configured. Once you have
identified the NIS domain, enter the domain name in the box provided
by this menu.

See the NIS Administrator's Guide for more information
on setting the NIS domain name.
.helpinst
Del=Cancel F1=Help ESC=Exit help 1=Forward 2=Back

