%% Kernel Debugger (KDB) Parameters
%C Kernel Debugger (KDB) Parameters
This tunable parameter is associated with the Kernel Debugger.
% KDBSECURITY
This parameter controls the ability
to use CTRL-ALT-D to enter kdb.
If the value is off (0),
then CTRL-ALT-D can be used to enter kdb.
If security is turned on (1),
then the kdb command must be used.
