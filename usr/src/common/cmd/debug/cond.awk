#ident	"@(#)debugger:cond.awk	1.2"
# This file is processsed by Msg.awk, GMsg.awk and
# GLab.awk.  They use a single tab as the field
# separator, so any #define or #undef must be separated
# by a tab from following tokens.
#undef	SDE_SUPPORT
