#ident	"@(#)pcintf:support/svr4/pcidossvr.232	1.1"

# Exec /bin/sh because user may be running csh, and the ':' and '#!/bin/sh'
# script prefixes are mutually incompatible.  This form should run on all
# systems, regardless of which script prefix is recognized.

exec /bin/sh -c '. /usr/pci/bin/environ; exec /usr/pci/bin/pcidossvr.232'
