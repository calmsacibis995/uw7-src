#
#	This file supplies default values for the LDAP command line tools.
#
#	Examples:
#	1)	If by default you wish to contact the host
#		myhost.myco.com on port 777, change the LDAP_HOST 
#		line to read
#			LDAP_HOST="myhost.myco.com:777"
#	2)	If by default you wish the search base to be
#		"o=mycompany,c=mycountry", change the LDAP_BASE_DN 
#		line to read
#			LDAP_BASE_DN="o=mycompany,c=mycountry"
#
#	The quote marks around the values are optional
#	No other processing of characters is performed,
#	so for example, there is no use of \ as an escape character.
#
#	A null string for LDAP_HOST implies the local machine.
#
LDAP_HOST=""
LDAP_BASE_DN=""
LDAP_BINDDN_SEARCH=""
LDAP_BINDDN_CHANGE=""
