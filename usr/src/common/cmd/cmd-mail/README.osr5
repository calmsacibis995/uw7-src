#ident "@(#)README.osr5	11.1"

To make the ported stuff for OSR5:

make -f Makefile.osr5

Then install the stuff, as root run:

make -f Makefile.osr5 local-install

You also need to modify /etc/default/mail (from c-client/mail)
to match your MMDF configuration (mmdftailor).
