There are 5 terminfo source files in this directory:

terminfo.src	represents the terminfo database as installed by the terminf
		package

terminfo.src.ls	represents additional (and replacement) entries installed
		by the langsup package

terminfo.src.osr5
euroinfo.src
brasinfo.src
		represents the terminfo src as delivered on OpenServer
		Use at your own discretion.

One could:
	install the OSR5 terminfo over the UnixWare 7 terminfo database
	install the OSR5 terminfo, then reinstall the UnixWare 7 terminfo

Most of the database entries are identical
