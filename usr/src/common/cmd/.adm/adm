#ident	"@(#)adm:common/cmd/.adm/adm	1.2.6.2"
#ident  "$Header$"
#
# The adm crontab file should contain startup of performance collection if
# the profiling and performance feature has been installed.
#
# If you are running accounting utilities comment out the
# following 2 entries by prepending a pound sign to each.
0 0 * * * > /var/adm/wtmp
0 0 * * * > /var/adm/wtmpx
