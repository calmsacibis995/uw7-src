#ident	"@(#)pos_localedef:common/cmd/pos_localedef/montbl/montbl_POSIX	1.1"
# "POSIX" locale database
# Use default values in montbl command (see montbl(1M) man page).
# No specifications are necessary.
